<!DOCTYPE html>
<html lang="en">
  <head>
    <title>journalite &mdash; The power of journaling</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "styles", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "journal", "data-turbo-track": "reload" , defer: true %>
    <%= favicon_link_tag asset_path("journalite-logo.svg") %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
  </head>
  <body id="<%= yield (:body_id) %>">
    <%= render partial: "partials/header" %>
    <%= yield %>
    <%= render partial: "partials/footer" %>
  </body>
</html>
